this is a emulator for chip-8.
the code is still in progress (Only enough instructions for the IBM Logo.ch8).
I am using Rust since there is better memory management.

For the rendering of the display, I used macroquad.
